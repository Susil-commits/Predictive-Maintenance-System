services:
  - type: web
    name: pms-backend
    runtime: docker
    dockerfilePath: ./Dockerfile.backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
      - key: PMS_API_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
